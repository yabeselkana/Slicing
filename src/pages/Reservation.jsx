import React from "react";
import ReservationContent from "../componets/Reservation";

const Reservation = () => {
  return (
    <section>
      <ReservationContent />
    </section>
  );
};

export default Reservation;
